<%= form_for @shift do |f| %>
    <p>
      <%= f.label :time_from %><br>
      <%= f.datetime_select :time_from %>
    </p>

    <p>
      <%= f.label :time_to %><br>
      <%= f.datetime_select :time_to %>
    </p>

    <p>
      <%= f.submit %>
    </p>

<% end %>